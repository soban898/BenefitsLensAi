services:
  - type: web
    name: benefitlens-ai-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
    healthCheckPath: /health
